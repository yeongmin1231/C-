#include <stdio.h>

int main() {
    char str[100];
    int length = 0;

    printf("문자열을 입력하세요: ");
    scanf("%s", str);

    while (str[length] != '\0') {
        length++;
    }

    printf("문자열의 길이: %d\n", length);
    return 0;
}
